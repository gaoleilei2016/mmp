    <el-table-column
      type="selection"
      width="30">
    </el-table-column>
    <%= render "hospital/home/render/orders_table_common_partial.html" %>
    <el-table-column
      prop="status"
      align="center"
      min-width="80"
      label="状态">
      <template slot-scope="scope">
        <template v-for="st in ordersStatus">
          <el-tag size="mini" :type="st.type" v-if="scope.row.status==st.value">{{st.label}}</el-tag>
        </template>
      </template>
    </el-table-column>